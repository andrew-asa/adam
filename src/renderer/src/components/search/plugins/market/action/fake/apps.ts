import { total_plugins as tp } from "./total_plugins";
import { new_plugins as np, recomend_plugins as rp } from "./finder_plugins";
export const total_plugins = tp
export const new_plugins = np
export const recomend_plugins = rp